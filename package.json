{
  "name": "shellgarden",
  "version": "0.1.0",
  "description": "Fixture-backed shell command examples with deterministic local transcripts.",
  "type": "module",
  "homepage": "https://github.com/rogerchappel/shellgarden#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/shellgarden.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/shellgarden/issues"
  },
  "bin": {
    "shellgarden": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "SECURITY.md",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit && node scripts/check-docs.mjs",
    "test": "npm run build && node --test tests/**/*.test.mjs",
    "smoke": "npm run build && node dist/index.js check fixtures/pass --format text && node dist/index.js report fixtures/pass --format json > /tmp/shellgarden-smoke-report.json",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm run build && npm test && npm run smoke && npm run package:smoke"
  },
  "keywords": [
    "cli",
    "shell",
    "fixtures",
    "doctest",
    "transcripts",
    "testing"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
