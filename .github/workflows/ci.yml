name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Verify ShellGarden
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck and docs check
        run: npm run check

      - name: Run tests
        run: npm test

      - name: Run smoke checks
        run: npm run smoke

      - name: Validate repository
        run: bash scripts/validate.sh
